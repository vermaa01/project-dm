Q1. 10M

list1 <- list("x", "y", "z")

list2 <- list("X", "Y", "Z", "x", "y", "z")

vec1 <- unlist(list1)

vec2 <- unlist(list2)

diff1 <- setdiff(vec1, vec2)

diff2 <- setdiff(vec2, vec1)

print("Elements in list1 but not in list2:")

print(diff1)

print("Elements in list2 but not in list1:")

print(diff2)

Q2. 20M (CSV)

import pandas as pd
from sklearn.preprocessing import StandardScaler
from sklearn.cluster import AgglomerativeClustering
import scipy.cluster.hierarchy as shc
import matplotlib.pyplot as plt

df = pd.read_csv("Wholesale customers data.csv")

X = df.select_dtypes(include=['number'])

X_scaled = StandardScaler().fit_transform(X)

plt.figure(figsize=(8,4))

dend = shc.dendrogram(shc.linkage(X_scaled, method='ward'))
plt.show()

hc = AgglomerativeClustering(n_clusters=3, linkage='ward').fit(X_scaled)

print("Cluster counts:", pd.Series(hc.labels_).value_counts())


