Q1. 10M

companies = data.frame(Shares = c("TCS", "Reliance", "HDFC", "Infosys", "Reliance"),price = c(3200, 1900, 1500, 2200, 1900))

companies

cat("After removing Duplicates", "\n")

companies = companies[duplicated(companies), ]

unique(companies)

Q2. 20M (CSV)

import pandas as pd
from sklearn.preprocessing import StandardScaler
from sklearn.cluster import AgglomerativeClustering
import matplotlib.pyplot as plt
import scipy.cluster.hierarchy as sch

df = pd.read_csv("Wholesale customers data.csv")

print("First 5 rows of dataset:")
print(df.head())

num = df.select_dtypes(include = ['number'])

if num.empty:
    raise ValueError("No numeric columns found in the dataset. Please check your CSV.")

num = num.fillna(num.mean())

scaler = StandardScaler()

X = scaler.fit_transform(num)

plt.figure(figsize = (8, 4))
plt.title("Customer Dendrogram")
dendrogram = sch.dendrogram(sch.linkage(X, method = 'ward'))
plt.xlabel("Customers")
plt.ylabel("Euclidean distances")
plt.show()

cluster = AgglomerativeClustering(n_clusters = 3, linkage = 'ward')

labels = cluster.fit_predict(X)

df['Cluster'] = labels

print("\nClustered data (first 10 rows):")
print(df.head(10))

